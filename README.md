# Cuore del Sud — One-Page Website

A simple, responsive one-page website for **Cuore del Sud**, an Italian export company from Battipaglia.

## Run locally

No build tools are required. Open the HTML file directly:

```bash
open index.html
```

Or double-click `index.html` in your file explorer.

To view the contact page locally, open:

```bash
open contact.html
```

Note: the contact page uses external image URLs (hotlinked), so an internet connection is required to load them.

## Publish with GitHub Pages

1. Push the repository to GitHub.
2. Go to **Settings → Pages**.
3. Under **Build and deployment**, select:
   - **Branch**: `main`
   - **Folder**: `/ (root)`
4. Save. GitHub Pages will publish the site automatically.

The contact page will be available at `/contact.html`.
